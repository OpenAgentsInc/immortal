# Copy the two site blocks into the operator-owned Caddy configuration and
# replace both example domains. Plain relay ports remain bound to 127.0.0.1.
relay-a.regtest.example.com {
	reverse_proxy 127.0.0.1:18080
}

relay-b.regtest.example.com {
	reverse_proxy 127.0.0.1:18081
}
